[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trustchain-sdk"
version = "0.1.0"
description = "Official TrustChain Python SDK for the public developer API"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Proprietary" }
authors = [{ name = "TrustChain" }]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["trustchain*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
