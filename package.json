{
  "name": "trustchain",
  "version": "0.0.0",
  "private": true,
  "description": "TrustChain monorepo — document trust cloud",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "blockchain"
  ],
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build -w @trustchain/config && npm run db:generate && npm run build -w @trustchain/database && npm run build -w @trustchain/backend && npm run build --workspaces --if-present",
    "compile:contracts": "npm run compile -w @trustchain/blockchain",
    "migrate:up": "npm run db:migrate:deploy -w @trustchain/database",
    "migrate:down": "echo \"Prisma does not support migrate down; use prisma migrate resolve / restore\"",
    "db:generate": "npm run db:generate -w @trustchain/database",
    "db:migrate": "npm run db:migrate -w @trustchain/database",
    "db:seed": "npm run db:seed -w @trustchain/database",
    "test:ai": "cd services/ai && PYTHONPATH=. python3 -m pytest -q",
    "test:ops": "npx tsc -p services/tsconfig.json && node services/ops-unit-check-dist.mjs",
    "ci": "npm run lint && npm run format:check && npm run build -w @trustchain/config && npm run db:generate && npm run build -w @trustchain/database && npm run typecheck && npm run build -w @trustchain/backend && npm run test:unit -w @trustchain/backend && npm run build -w @trustchain/web && npm run build -w @trustchain/admin && npm run test:unit -w @trustchain/extension && npm run build -w @trustchain/extension && npm run test:unit -w @trustchain/mobile && npm run compile -w @trustchain/blockchain && npm run test -w @trustchain/blockchain && npm run test:ai && npm run test:ops"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.18.2"
  },
  "dependencies": {
    "type-detect": "^4.1.0"
  }
}
