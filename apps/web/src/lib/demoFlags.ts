/**
 * Phase 1 demo toggles — flip these after viva without deleting features.
 * Blockchain wallet routes/API (`/wallets`, walletApi, WalletDashboardPage) stay in the codebase.
 */
export const SHOW_BLOCKCHAIN_WALLET_NAV = false;
